dist/
api-baseline/

# Generated by scripts/build-map-data.mjs (`pnpm build:map-data`), which emits
# the JSON assets, README.md and PROVENANCE.json — the last of which records
# each asset's byte count and sha256, so reformatting any of them makes the
# audit trail wrong. The JSON is also minified and ships inside the published
# package: prettier roughly doubles it. types.ts is hand-written and stays
# checked; the generator never overwrites it.
src/map/data/*
!src/map/data/types.ts

# Generated by pnpm; every other repo in the workspace ignores it too.
pnpm-lock.yaml

# The five AI-instruction surfaces. scripts/gen-ai-core.mjs writes the same
# DGMO-AI-CORE block into all of them, and
# tests/language-reference-examples.test.ts asserts the block is byte-identical
# across every one — so formatting the three prettier can parse, while
# .cursorrules and .windsurfrules have no extension and are skipped, breaks the
# single-source invariant. Caught by the pre-push gate on 2026-08-29, not by
# reasoning: 9,281 tests passed and this one failed.
.cursorrules
.windsurfrules
.github/copilot-instructions.md
SKILL.md
docs/ai-integration.md
