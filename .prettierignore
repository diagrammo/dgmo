dist/
api-baseline/

# Generated by scripts/build-map-data.mjs (`pnpm build:map-data`), which emits
# the JSON assets, README.md and PROVENANCE.json — the last of which records
# each asset's byte count and sha256, so reformatting any of them makes the
# audit trail wrong. The JSON is also minified and ships inside the published
# package: prettier roughly doubles it. types.ts is hand-written and stays
# checked; the generator never overwrites it.
src/map/data/*
!src/map/data/types.ts

# Generated by pnpm; every other repo in the workspace ignores it too.
pnpm-lock.yaml
